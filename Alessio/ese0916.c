#include <stdio.h>
#include <stdlib.h>

int * doubler(int *, int);

void main(){
    // tipo int pesa 4 bytes
    int n, check = 1;
    int * z;
    while(check != 0){
        printf("Inserire il valore intero di n, oppure 0 per interrompere.\nScelta: ");
        scanf("%d", &n);
        doubler()
    }
}